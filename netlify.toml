# Netlify config for the Vite React app inside /frontend

[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Single Page App routing: send all paths to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
